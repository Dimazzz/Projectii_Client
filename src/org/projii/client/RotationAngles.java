package org.projii.client;

public class RotationAngles
{
	 public float prevAngle,nextAngle;
	 public RotationAngles(float prevAngle,float nextAngle)
	 {
		 this.prevAngle=prevAngle;
		 this.nextAngle=nextAngle;
	 }
}
